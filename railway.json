{
  "$schema": "https://railway.com",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "runtimeConfig": {
      "PHP_VERSION": "8.3"
    },
    "startCommand": "php artisan migrate --force && frankenphp run --config /Caddyfile"
  }
}
